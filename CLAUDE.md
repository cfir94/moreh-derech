# מורי דרך — מערכת מאוחדת לתלמידי קורס מורי דרך

## מטרת הפרויקט

אתר/מערכת אחת שמרכזת את כל מה שתלמיד בקורס מורי דרך צריך:

- **מאגר שאלונים** — שאלונים אמריקאיים (רב-ברירה) לפי קטגוריות מהחומר הנבחן.
- **דוחות הדרכה** — לפי הפורמט שדורש משרד התיירות, כולל הדרכה כתובה איך לכתוב
  דוח, וסימולציית "בניית מסלול" כמו במבחן הסופי.
- **סרטונים מומלצים** — קישורים לסרטוני יוטיוב וכו' לפי נושא.
- **צירי זמן** — לפי נושאים: תקופות היסטוריות, תולדות הנצרות, תולדות ירושלים
  וכו'.
- **מפה אינטראקטיבית** — מפת ישראל עם נקודות ציון והסברים למורה דרך.
- **התחברות ומעקב אישי** — כניסה עם שם+מייל בלבד (ללא סיסמה), ומעקב אחרי מה
  כל משתמש תרגל, מה טעה בו, ומה צריך לחזור עליו.

זהו איחוד של כמה פרויקטים חלקיים שכבר קיימים אצל בעל הפרויקט (שאלונים, צירי
זמן, מערכת מפה) — הם ישולבו בהדרגה, סקציה אחת בכל פעם.

## סטאק טכני

- Next.js 16 (App Router) + TypeScript + Tailwind CSS v4
- Prisma + SQLite (`prisma/schema.prisma`, DB בקובץ `prisma/dev.db`)
- אימות: session cookie פשוט (`session_user_id`), ללא סיסמה — ראו
  `src/lib/session.ts` ו-`src/app/actions/auth.ts`

## מבנה תיקיות

```
src/
  app/
    page.tsx              דף בית עם קישורים לכל הסקציות
    login/                עמוד כניסה (שם + מייל)
    me/                   אזור אישי (דורש התחברות)
    quizzes/               placeholder לסקציית השאלונים
    guide-reports/         placeholder לסקציית דוחות ההדרכה
    videos/                 placeholder לסקציית הסרטונים
    timelines/               placeholder לסקציית צירי הזמן
    map/                     placeholder לסקציית המפה
    actions/auth.ts          server actions: login / logout
  components/
    Navbar.tsx               ניווט ראשי, מציג מצב התחברות
    PlaceholderSection.tsx    קומפוננטת placeholder לסקציות שטרם שולבו
  lib/
    prisma.ts                 Prisma client singleton
    session.ts                 קריאה/יצירה/מחיקה של session cookie
prisma/
  schema.prisma                מודלים: User, QuizAttempt, QuestionResult (שלד)
```

## מוסכמות

- כל ה-UI בעברית, `dir="rtl"` על ה-`<html>` (מוגדר ב-`src/app/layout.tsx`).
- פונט Rubik (תומך עברית) דרך `next/font/google`.
- מודל ה-`User` מזוהה לפי `email` ייחודי — אין סיסמה, ההתחברות היא
  find-or-create לפי מייל (`upsert` ב-`src/app/actions/auth.ts`).
- אין עדיין middleware גורף שמגן על נתיבים — הגנה נעשית per-page עם
  `getCurrentUser()` + `redirect("/login")` (ראו `src/app/me/page.tsx`).

## רודמאפ — שילוב חומרים קיימים (כל שלב = משימה נפרדת)

לא לבצע הכל בבת אחת. הבעלים יספק קישורים לריפואים/תיקיות קיימות בכל שלב:

1. **שאלונים** — שילוב מאגר שאלונים קיים, מסך תרגול, ומעקב טעויות שממלא את
   המודלים `QuizAttempt`/`QuestionResult`.
2. **מפה אינטראקטיבית** — שילוב מערכת מפה קיימת תחת `/map`.
3. **צירי זמן** — שילוב צירי זמן קיימים תחת `/timelines`.
4. **דוחות הדרכה** — תבנית משרד התיירות + סימולציית מסלול, תחת
   `/guide-reports`.
5. **סרטונים** — רשימת קישורים מסודרת לפי נושא, תחת `/videos`.
6. **דשבורד מעקב אישי מלא** — הרחבת `/me` להציג נתוני התקדמות אמיתיים מה-DB.

## פקודות שימושיות

```bash
npm run dev              # שרת פיתוח
npx prisma studio         # עיון/עריכה ויזואלית של ה-DB
npx prisma migrate dev    # יצירת מיגרציה חדשה אחרי שינוי סכמה
```
