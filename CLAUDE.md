# אבן דרך למורי דרך — מערכת מאוחדת לתלמידי קורס מורי דרך

## מטרת הפרויקט

אתר/מערכת אחת שמרכזת את כל מה שתלמיד בקורס מורי דרך צריך:

- **מאגר שאלונים** — שאלות רב-ברירה לפי נושאים, כולל שאלות ממבחני הרישוי.
- **מבחנים מלאים** — שישה מועדי רישוי בתנאי אמת, עד שלוש שפות (`/exams`).
- **צירי זמן** — צירי זמן אינטראקטיביים לפי נושא היסטורי.
- **מפה אינטראקטיבית** — מפת מורשת ישראל בשכבות.
- **מעקב אישי** — חשבון Supabase משותף עם משחק "אבן דרך", כניסה עם
  שם+מייל (ללא סיסמה), ומעקב ברמת השאלה הבודדת:
  מה נענה, במה טעו, ומה עוד ממתין לחזרה.
- **דוחות הדרכה** ו**סרטונים** — עדיין placeholder, ממתינים לחומר מקור.

## אתר סטטי עם אימות משותף

האתר מתארח ב-GitHub Pages (`https://cfir94.github.io/moreh-derech/`) —
אין שרת Next.js, אבל האימות מתבצע ישירות מול פרויקט ה-Supabase שכבר משמש
את `cfir94/israel-geo-game`. זה קבע את הארכיטקטורה:

- Next.js 16 (App Router) + TypeScript + Tailwind v4, עם `output: "export"`
  ו-`basePath: "/moreh-derech"` (`next.config.ts`). הבנייה מייצרת `out/`.
- האימות נמצא ב-`src/lib/cloudAuth.ts`. כתובת Supabase, מפתח ה-anon, מלח
  גזירת הסיסמה ומפתח הסשן חייבים להישאר זהים למשחק. מאחר ששני האתרים
  מתארחים תחת `cfir94.github.io`, גם ה-session ב-localStorage משותף בפועל.
- התקדמות השאלונים של האתר עדיין מקומית, אבל מופרדת לפי `user_id`, כך
  שמשתמשים שונים באותו דפדפן לא רואים זה את נתוני זה. נתונים מהגרסה הישנה
  מועברים לחשבון הראשון שנכנס. התקדמות המשחק ממשיכה להסתנכרן בענן בנפרד.

## איך התוכן הגיע לכאן — חשוב לפני עריכת תוכן

לבעלים היו כמה אפליקציות Vite/React עצמאיות (שאלונים, צירי זמן, מפה), חלקן
ריפואי GitHub וחלקן ZIP. **הנתונים מהן עברו פורט מלא לתוך האתר** — הם אינם
אפליקציות נפרדות יותר:

- `src/data/quizzes/*.ts` — נוצרו אוטומטית מ-`quizData.ts` של ארבעת
  פרויקטי השאלונים (`/workspace/cfir94/{geology,history,iron-age}-quiz`,
  `/workspace/sources/tourguidequiz`). קבצים אלה **generated — לא לערוך ידנית**.
- `src/data/quizzes/past-exams.ts` ו-`src/data/exams/*.ts` — שאלות ממבחני
  הרישוי הרשמיים של משרד התיירות, עם התשובות הרשמיות. נוצרים **יחד** ע"י
  `tools/build_exams.py` (ראו "מבחני רישוי" למטה).
- `src/data/timelines/*.ts` — נוצרו מ-`biblicaltimelines` ומ-
  `egyptcanaantimeline`.
- תמונות השאלונים הועתקו ל-`public/quiz-images/<quiz>/`.

ה-UI נכתב מחדש מקומית (`src/components/quiz/QuizRunner.tsx`,
`src/components/timeline/TimelineViewer.tsx`) בעיצוב של האתר, כדי שהחוויה
תהיה אחידה ולא "קפיצה" לממשק זר.

**היוצא מן הכלל — המפה**: `israel-heritage-map` היא אפליקציית MapLibre שלמה
(vector tiles, service worker, ~90MB נתונים). היא נשארה כפי שהיא תחת
`public/embeds/map/` ורצה ב-`<iframe>` בתוך `/map` — כך המשתמש נשאר בתוך
האתר עם הניווט שלו, בלי לשכתב מנוע מפות שלם.

### מבחן מלא בתנאי אמת (`/exams`)

מעבר לתרגול שאלה-שאלה, יש **מצב מבחן** — מבחן שלם כפי שמשרד התיירות מעביר
אותו (`src/components/exam/ExamRunner.tsx`):

- **התשובה לא נחשפת בזמן המענה.** אין משוב על לחיצה — רק סימון הבחירה.
- <span dir="ltr">5</span> שאלות בעמוד, ניווט חופשי קדימה ואחורה, ואפשר לשנות
  תשובה עד ההגשה. "דף התשובות" בתחתית מראה אילו שאלות כבר נענו ומקפיץ לכל אחת.
- הגשה מפורשת; אם נשארו שאלות ריקות מוצגת אזהרה עם מספריהן לפני הגשה סופית.
- אחרי ההגשה: ציון, כוכבים, כל השאלות עם התשובה הנכונה ועם התשובה שנבחרה,
  וגם **ההשלמה הרשמית של החלק הראשון** של כל פריט.
- **עד שלוש שפות** (עברית/אנגלית/ערבית) — המבחן מתפרסם בכמה מהדורות, וכל
  אחת נמצאת ב-`src/data/exams/<slug>.ts`. שפה מוצעת רק אם הטקסט שלה יצא מלא
  ונקי (שדה `languages`); ערבית של קיץ 2023 נפסלה אוטומטית. ה-chrome של האתר
  נשאר עברית; רק תוכן המבחן וכיוון הכתיבה מתחלפים.

**המועדים הקיימים** (`tools/build_exams.py` → `SITTINGS`): חורף 2021, חורף
2022, קיץ 2023, יולי 2024, ינואר 2025, יולי 2025.

**האינטגרציה עם המעקב**: תשובות במבחן נרשמות תחת השאלון `past-exams` ולפי
מזהה השאלה המקבילה בו (`quizId` = `id_base` של המועד + מספר הפריט), ולכן טעות
במבחן נכנסת ל"תרגול טעויות" הרגיל. לכן קבצי `src/data/exams/*.ts` ו-
`src/data/quizzes/past-exams.ts` **נוצרים יחד** ע"י `tools/build_exams.py` —
אין לערוך אף אחד מהם ידנית.

### מבחני רישוי רשמיים (`tools/build_exams.py` + `tools/parse_exam.py`)

הבעלים סיפק PDF-ים של מבחני הרישוי 2020–2025 (עברית/אנגלית/ערבית, חלק א'
רב-ברירה וחלק ב' פתוח). `tools/build_exams.py` מחלץ מהם את החלק הרב-ברירתי
בכל שפה, וכותב גם את מצב המבחן וגם את שאלון התרגול.

הכלל שקובע מה נכנס: **מפתח תשובות רשמי.** קיץ 2020, קיץ 2022, קיץ 2024, חורף
2025 וקיץ 2025 קיימים אצלנו כשאלות בלבד — ותשובה מנוחשת לקראת מבחן רישוי
גרועה מכלום, אז הם לא נכללים.

נקודות שחשוב להכיר לפני הרצה על מבחן חדש:

- כל פריט מורכב משני חלקים: השלמת משפט (עם התשובה שהמפתח מדפיס בסוגריים)
  ושאלת הבנה עם 4 תשובות. **שניהם נשמרים** — במצב מבחן שניהם מוצגים, ורק
  השני נבחן.
- **התשובה הנכונה מסומנת ויזואלית**: ב-2021–2023 ע"י **טקסט מודגש** (font
  `David-Bold`), וב-2024–2025 ע"י **מלבן הדגשה צהוב** (`(1,1,0)`). הסקריפט
  מזהה את שתי השיטות. סימון מודגש חייב לכסות את רוב השורה **ולשבת בתוכה**
  לפי חפיפה של יותר מחצי גובה הגליף — חלון רפוי יותר גרם למפתח של חורף 2022
  לסמן שתי תשובות כמעט בכל שאלה.
- **פירסור לפי פריטים ממוספרים** על זרם שורות אחד. הגרסה הקודמת סרקה "רצפי
  תשובות" ואיבדה בשקט אופציה שהתווית והטקסט שלה נפלו לשורות נפרדות, אופציה
  שגלשה, ופריט שנחתך בין עמודים (יולי 2024 יצא 29 שאלות במקום 33).
- **תוויות התשובות מזוהות לפי מיקום ולא לפי אלפבית** — מהדורות באנגלית
  ובערבית משתמשות לפעמים ב-א'-ד', ופריט אחד בערבית של ינואר 2025 מערבב
  ("א ב ג د"). יש גם מעבר סלחני שני שמקבל תווית מחוץ לסדר, לפריט שבו התוויות
  המודפסות חוזרות על עצמן ("أ ب ب د").
- **טקסט נבנה מהגליפים של כל מילה**, לא מהיפוך המחרוזת: היפוך מחרוזת שובר את
  ליגטורות לאם-אליף בערבית (`لإ` הופך ל-`إل`), כי הפונט ממפה גליף אחד לשני
  תווים. סימני ניקוד ערביים נמחקים — הם מגיעים כתיבות ברוחב אפס ומפצלים מילה
  לשתיים. גם `fix_bidi_runs()` נחוץ, כי קריאה מימין לשמאל הופכת ספרות ולטינית
  (1917 → 7191).
- **אימות בין מהדורות**: מועד שיש לו מפתח ביותר משפה אחת בודק את עצמו —
  התשובה המסומנת חייבת לשבת באותו אינדקס בכל המהדורות. אי-הסכמה = שגיאת
  פרסינג, והשאלה נזרקת. (מהדורה שמסמנת פחות מ-60% מהפריטים נחשבת "בלי מפתח"
  ואינה משתתפת באימות — הדגשה אקראית אחת לא תפסול שאלה תקינה.)
- **מה שלא מתפרסר נקי — נזרק, לא מנוחש.** שאלה נכללת רק אם יש בדיוק 4 תשובות
  ובדיוק תשובה אחת מסומנת. לכן חלק מהמועדים קצרים מ-33 שאלות.
- **שפה נפסלת אוטומטית** אם הטקסט שלה יצא פגום: יותר מ-8% מילים בנות אות אחת.
  כך נפסלה הערבית של קיץ 2023 (19%), שקובץ ה-PDF שלה מפזר אותיות ברוחב אפס.
- `tools/parse-exams.py` הוא **הסקריפט הישן**, שנשאר רק כי הקטגוריה
  "מועד נוסף 2024" בשאלון עדיין מגיעה ממנו. אין להריץ אותו על מועדים חדשים.

הרצה: `pip install pdfplumber` ואז מהשורש:
`python3 tools/build_exams.py --pdfs /path/to/exam/pdfs`
(מוסיפים מועד חדש כרשומה ב-`SITTINGS`, עם `id_base` חדש).

## מערכת המעקב (`src/lib/progress.ts`)

מפתח `md_progress_v2:<user_id>` ב-localStorage למשתמש מחובר, או
`md_progress_v2` לאורח, ובו שני חלקים:

- `attempts[]` — שורה לכל סבב תרגול שהושלם (לסטטיסטיקה והיסטוריה).
- `questions{}` — מצב לכל שאלה, במפתח `"<quizSlug>:<questionId>"`, כולל
  `seen` / `wrong` / `lastCorrectAt` / `lastWrongAt`.

**הלוגיקה המרכזית**: שאלה נחשבת "ממתינה לחזרה" כל עוד התשובה **האחרונה**
עליה הייתה שגויה (`needsReview()`). לכן מענה נכון מאוחר יותר מוציא אותה
מהתור — אבל היסטוריית הטעויות (`wrong`) נשמרת.

מכאן `/quizzes/review` — מסך שמתרגל בדיוק את השאלות האלה, חוצה-שאלונים.
שים לב ל-`sourceQuiz` על `Question`: במצב חזרה השאלות מגיעות מכמה שאלונים,
והשדה הזה מבטיח שהתוצאה נזקפת לשאלון המקורי ולא ל"review" — בלי זה מענה
נכון בחזרה לא היה מנקה את השאלה (באג שנתפס ותוקן).

## מבנה

```
src/
  app/
    page.tsx                 דף בית
    login/  me/               כניסה ואזור אישי (מעקב מלא)
    exams/                    אינדקס המועדים
      [slug]/                 מועד בתנאי אמת (הגשה בסוף, בחירת שפה)
    quizzes/                  אינדקס שאלונים
      [slug]/                 שאלון בודד (generateStaticParams)
      review/                 תרגול טעויות חוצה-שאלונים
    timelines/                אינדקס + biblical/ + egypt-canaan/
    map/                      iframe של מפת המורשת
    guide-reports/  videos/    placeholders
  components/
    exam/ExamRunner.tsx        מנוע המבחן המלא (בלי חשיפת תשובות)
    quiz/QuizRunner.tsx        מנוע השאלונים (setup → שאלות → תוצאות)
    timeline/TimelineViewer.tsx צירי זמן אינטראקטיביים
    Navbar.tsx  HomeProgress.tsx  PlaceholderSection.tsx
  contexts/UserContext.tsx
  hooks/useProgress.ts         קריאת המעקב בצד לקוח בלבד
  lib/  progress.ts  localAuth.ts  basePath.ts
  data/ quizzes/  timelines/  exams/   נתונים generated
public/
  quiz-images/<quiz>/           תמונות השאלות
  embeds/map/                    אפליקציית המפה כפי שהיא
```

## מוסכמות ועיצוב

- UI בעברית, `dir="rtl"`, פונט **Heebo**.
- **שפת העיצוב לקוחה מ-`cfir94/israel-geo-game` ("אבן דרך")** — פרויקט משחק
  החידון של אותו בעלים, לאותו קהל. הרעיון: זה נראה כמו **משחק**, לא כמו מסמך.
  המאפיינים המרכזיים, כולם ב-`src/app/globals.css`:
  - **רקע מואר**: שני washes רדיאליים (טורקיז + כחול) מעל גרדיאנט אנכי. לא רקע שטוח.
  - **כרטיסים שקופים למחצה** (`--card`) מעל הרקע הזה — לא לבן אטום, אחרת הם
    "מנקבים חורים" ברקע.
  - **צבע לכל נושא** (`src/lib/domains.ts`): כל שאלון/סקציה מקבל צבע ואייקון,
    והצבע נחשף כ-`--mc` ומופיע כ**כדור מטושטש** בפינת הכרטיס ובפס ההתקדמות.
  - **כפתור ראשי**: גלולה עם גרדיאנט `teal→blue` וזוהר בצבע הכפתור.
  - **כותרות**: משקל 900, ו-`.grad-text` לכותרות ראשיות (גרדיאנט על הטקסט).
  - **תשובות בשאלון**: נכונה = wash ירוק, שגויה = wash אדום + `shake`,
    השאר דוהות ל-0.28 — בדיוק כמו `.ans` במשחק.
  - **לחיצות**: `active:scale-95` בכל מקום; מסכים נכנסים עם `.screen-in`.
- **מצב כהה** הוא אזרח מן המניין (נאוי עמוק), לא פילטר — כל הטוקנים מוגדרים
  מחדש תחת media query ותחת `[data-theme="dark"]`.
- **אין להשתמש בצבעי Tailwind גולמיים** (`text-neutral-600` וכו') — רק
  בטוקנים (`text-txt-dim`, `bg-card`, `border-line`, `text-mc`...), אחרת
  מצב כהה נשבר.
- קריאה ל-localStorage תמיד ב-`useSyncExternalStore` (דרך `useProgress` /
  `UserContext`), אחרת יש hydration mismatch מול ה-HTML הסטטי.
- נתיבי assets ידניים חייבים `withBasePath()` מ-`src/lib/basePath.ts`.
- הפריסה היא **עמודה ממוקדת** ולא מסמך רחב — המשחק הוא 560px; כאן
  `max-w-2xl` למסכי שאלון ו-`max-w-5xl` לרשתות כרטיסים.

## רודמאפ פתוח

1. **דוחות הדרכה** ו**סרטונים** — ממתינים לחומר מקור.
2. תמונות שאלון "החי והצומח" — 79 שאלות הצביעו על שרת אחסון של Manus שאינו
   קיים; הקבצים לא היו ב-ZIP. השאלון עובד, רק בלי תמונות. אם יימצאו הקבצים,
   להעתיק ל-`public/quiz-images/flora-fauna/` ולעדכן את הנתיבים בסקריפט
   החילוץ.
3. סנכרון התקדמות האתר בין מכשירים — אפשר להוסיף בטבלה נפרדת באותו Supabase;
   האימות עצמו כבר משותף ומסונכרן.

## פקודות

```bash
npm run dev     # פיתוח — לגשת ל-http://localhost:3000/moreh-derech/
npm run build    # static export ל-out/
```

פריסה: GitHub Actions (`.github/workflows/deploy.yml`) בונה ודוחף ל-
`gh-pages` בכל push ל-main.
